<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <!-- 상단 네비 -->
  <header class="topbar">
    <div class="topbar-left">
      <a href="/" class="logo-small">RPS ROULETTE</a>
    </div>
    <nav class="topbar-right">
      <% if (!currentUser) { %>
        <button type="button" onclick="location.href='/login'">로그인</button>
        <button type="button" onclick="location.href='/signup'">회원가입</button>
      <% } else { %>
        <% if (currentUser.role === 'admin') { %>
          <button type="button" onclick="location.href='/admin'">관리자 페이지</button>
        <% } else { %>
          <button type="button" onclick="location.href='/mypage'">마이페이지</button>
        <% } %>
        <form action="/logout" method="post" style="display:inline;">
          <button type="submit">로그아웃</button>
        </form>
      <% } %>
    </nav>
  </header>

<div class="stage">
  <div class="auth-card">

    <div class="auth-title">회원가입</div>
    <div class="auth-subtitle">아이디와 비밀번호를 입력해 주세요.</div>

    <form action="/signup" method="POST">

      <div class="auth-field">
        <label for="username">아이디</label>
        <input type="text" id="username" name="username" placeholder="아이디" required />
      </div>

      <div class="auth-field">
        <label for="password">비밀번호</label>
        <input type="password" id="password" name="password" placeholder="비밀번호" required />
      </div>

      <button type="submit" class="auth-submit accent">회원가입</button>
    </form>

    <div class="auth-switch">
      이미 계정이 있으신가요?
      <a href="/login">로그인 하러가기</a>
    </div>

  </div>
</div>

</body>
</html>
